{
  "name": "Form",
  "import": "import { Form, FormField, FormItem, FormLabel, FormControl, FormMessage } from '@/components/ui/form'",
  "category": "form",
  "description": "M3 form wrapper with react-hook-form integration. Labels use body-small (12px/500) text-on-surface-variant. Error messages use error color token.",
  "tags": ["form", "validation", "fields", "submit"],
  "variants": [],
  "props": {
    "form": {
      "type": "UseFormReturn",
      "required": true,
      "description": "React Hook Form instance from useForm()"
    },
    "onSubmit": {
      "type": "(values: T) => void",
      "required": true,
      "description": "Form submission handler"
    }
  },
  "example": "<Form {...form}>\n  <form onSubmit={form.handleSubmit(onSubmit)} className=\"space-y-[var(--grid-6)]\">\n    <FormField\n      control={form.control}\n      name=\"email\"\n      render={({ field }) => (\n        <FormItem>\n          <FormLabel className=\"text-[12px] leading-4 font-medium text-on-surface-variant\">Email</FormLabel>\n          <FormControl>\n            <Input placeholder=\"user@example.com\" {...field} />\n          </FormControl>\n          <FormMessage className=\"text-[12px] text-error\" />\n        </FormItem>\n      )}\n    />\n    <Button type=\"submit\" className=\"rounded-full\">Submit</Button>\n  </form>\n</Form>",
  "never": [
    "Never build forms without react-hook-form -- use Form wrapper",
    "Never use raw <label> -- use FormLabel with M3 label-medium styling",
    "Never show errors without FormMessage -- it handles validation display with error color token"
  ],
  "dependencies": ["react-hook-form", "@hookform/resolvers", "zod"]
}
