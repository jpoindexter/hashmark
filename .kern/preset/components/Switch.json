{
  "name": "Switch",
  "import": "import { Switch } from '@/components/ui/switch'",
  "category": "form",
  "description": "M3 toggle switch built on Radix. 52x32px track with rounded-full shape. Uses primary color when on, surface-variant when off.",
  "tags": ["form", "toggle", "switch", "boolean"],
  "variants": [],
  "props": {
    "checked": { "type": "boolean", "required": false },
    "onCheckedChange": { "type": "(checked: boolean) => void", "required": false },
    "disabled": { "type": "boolean", "required": false, "default": "false" },
    "id": { "type": "string", "required": false }
  },
  "example": "<div className=\"flex items-center justify-between min-h-12\">\n  <label htmlFor=\"notifications\" className=\"text-[16px] leading-6 text-on-surface\">Notifications</label>\n  <Switch id=\"notifications\" />\n</div>",
  "never": [
    "Never use Checkbox for instant-apply toggles -- use Switch",
    "Never hardcode toggle colors -- use primary and surface-variant semantic tokens",
    "Never make switch row shorter than 48px touch target"
  ],
  "dependencies": ["@radix-ui/react-switch"]
}
