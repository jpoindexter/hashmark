{
  "name": "Textarea",
  "import": "import { Textarea } from '@/components/ui/textarea'",
  "category": "form",
  "description": "M3 multi-line text input with outlined or filled style. Uses rounded-xs (4px) corners and outline token for border.",
  "tags": ["form", "text", "multiline", "input"],
  "variants": [],
  "props": {
    "placeholder": { "type": "string", "required": false },
    "rows": { "type": "number", "required": false, "default": "4" },
    "disabled": { "type": "boolean", "required": false, "default": "false" },
    "className": { "type": "string", "required": false }
  },
  "example": "<Textarea placeholder=\"Enter description\" rows={4} />",
  "never": [
    "Never use raw <textarea> -- always use <Textarea>",
    "Never hardcode border colors -- use outline token",
    "Never use sharp corners -- use rounded-xs (4px) minimum"
  ],
  "dependencies": []
}
