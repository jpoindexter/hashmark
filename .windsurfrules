# Windsurf Rules

# Auto-generated by hashmark (https://hashmark.md)

## Project: Next.js 16.1.6 + TypeScript + Tailwind CSS

## Coding Rules

1. Use existing components (33 available). Never create duplicates.
2. Use semantic design tokens (bg-primary, text-foreground). No hardcoded colors.
3. Default to Server Components. Use 'use client' only when needed.
4. Type all parameters. Never use 'any'.

## Components

app: OGImage, TwitterImage
dashboard: UpgradeButton, ComplexityPage, ConnectRepoDialog, DashboardBreadcrumbs, PlanUsageSection, DashboardShellWrapper, FilesPage, IntelligencePage, RepoCard, RepoSettingsPage, RepoSubNav, ReposPage, RuleCard, RuleDialog, ScanHistoryPage, ScanResultsTables, SearchDialog, SettingsPage, TrialBanner
landing: CliSection, Footer, Formats, Hero, HowItWorks, PricingTable
shared: LoginCard, OAuthButtons, StatusBadge, UpgradeGate
components: ThemeProvider, ThemeToggle

## Hooks

- useScanPolling from "@/hooks/use-scan-polling"
- useSearch from "@/hooks/use-search"

## API Routes

POST /api/billing/checkout [auth]
POST /api/billing/portal [auth]
POST /api/billing/webhook
GET /api/repos [auth]
POST /api/scan/:repoId [auth]
GET /api/scan/:repoId/download [auth]
GET /api/scan/:repoId/latest [auth]
GET /api/search [auth]
POST /api/webhooks/github

## Database

prisma models:
- Account: id, userId, type, provider, providerAccountId
- Session: id, sessionToken, userId, expires
- VerificationToken: identifier, token, expires
- User: id, name, email, emailVerified, image
- Repository: id, userId, githubRepoId, name, fullName
- Scan: id, repositoryId, results, fileCount, lineCount
- GeneratedFile: id, scanId, fileName, content, tokenCount
- CustomRule: id, userId, name, description, rule
- SearchChunk: id, repositoryId, scanId, sectionHeading, sectionType

## Commands

dev: npm run dev
build: npm run build
test: npm test
lint: npm run lint
