# GEMINI.md

> Auto-generated by [hashmark](https://hashmark.md) — Context for Google Gemini CLI

## Project

**Stack**: TypeScript
**Size**: 64 files, 14,023 lines
**Components**: 0

## Commands

```bash
```

## Coding Rules

- Use semantic design tokens: `bg-primary`, `text-foreground`, `border-border`.
- Never hardcode color values.
- Type all parameters. Avoid `any`.

## Architecture

### AI Automation Hooks

- **file_edit**: `npx tsc --noEmit` (for `**/*.{ts,tsx}`)
- **file_edit**: `npm test -- {{file}}` (for `src/**/*.ts`)
- **session_start**: `ls -d node_modules || npm install`
- **file_create**: `mkdir -p src/components/ui` (for `src/components/ui/*.tsx`)
